/*Meteor.startup( function () {
	if (Globals.find().count > 0) {
		var cars = Globals.find({});
		var carCount = cars.length;

		console.info('Server Globals Count');
		console.info(carCount);

		for (var i = 0; i < carCount; i++) {
			
		}
	}

});*/